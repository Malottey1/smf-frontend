{
  "name": "numeric-technologies",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Workspace root. apps/smf-frontend is the regulated GxP Console application (separate GxP validation scope). apps/www is the public corporate marketing site — deliberately outside that scope. See each app's own README.",
  "scripts": {
    "dev:app": "pnpm --filter smf-frontend dev",
    "dev:www": "pnpm --filter www dev",
    "build:app": "pnpm --filter smf-frontend build",
    "build:www": "pnpm --filter www build",
    "test:app": "pnpm --filter smf-frontend test"
  },
  "engines": {
    "node": ">=18.17"
  }
}
